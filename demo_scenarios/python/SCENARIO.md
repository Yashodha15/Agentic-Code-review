# Demo 01 - Python correctness

Expected review focus: correctness and testing, with control-flow, boundary-cases,
error-handling, coverage-gaps, edge-case-design, and regression-tests subagents.

The percentage calculation intentionally treats a value such as `15` as `1500%`.
Open a pull request into `main` and let the reviewer identify the contract ambiguity,
missing validation, and absent tests.
