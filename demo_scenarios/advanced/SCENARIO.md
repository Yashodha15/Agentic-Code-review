# Demo 10 - Advanced full-stack fan-out

Expected review focus: all five lead agents and the maximum policy-allowed set of
subagents. The branch combines Angular, React, TypeScript, JavaScript, Python, FastAPI,
security-sensitive paths, persistence, schema, and non-test production changes.

Use this PR last. Explain that deterministic planning selects the lanes before the LLM
runs, policy caps the total delegated calls, agents execute concurrently, and the fan-in
barrier waits for all completed, skipped, or failed branches before verification.

