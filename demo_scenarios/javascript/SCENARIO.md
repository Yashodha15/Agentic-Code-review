# Demo 02 - JavaScript asynchronous behavior

Expected review focus: correctness and testing agents plus their subagents.

The cache stores a rejected promise forever and accepts empty identifiers. The PR
demonstrates asynchronous error handling, retry behavior, input boundaries, and
missing regression coverage.
