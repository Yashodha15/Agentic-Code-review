# Demo 04 - React hooks and rendering

Expected review focus: frontend, correctness, and testing. Frontend subagents should
cover hooks lifecycle, rendering, browser safety, and accessibility.

The effect ignores query changes, requests are not cancelled, the query is not encoded,
errors are ignored, and list items have no stable key. This is the first demo that makes
the frontend specialist lane and its child tasks visible.
