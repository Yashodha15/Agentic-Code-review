import { Component, computed, input } from '@angular/core'; import { Tone } from '../../core/models';
@Component({selector:'ui-status-pill',standalone:true,template:`<span class="pill" [class]="'pill '+resolvedTone()"><i></i><ng-content /></span>`,styleUrl:'./status-pill.component.scss'}) export class StatusPillComponent{readonly tone=input<Tone>();readonly status=input('');readonly resolvedTone=computed<Tone>(()=>this.tone()??({completed:'success',verified:'success',running:'info',queued:'warning',failed:'danger',canceled:'danger',critical:'danger',high:'danger',medium:'warning',skipped:'neutral'}[this.status()] as Tone??'neutral'))}
