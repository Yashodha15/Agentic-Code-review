import { Component, inject } from '@angular/core';
import { NotificationService } from '../../core/notification.service';

@Component({selector:'ui-notification-stack',standalone:true,template:`<aside class="stack" aria-live="polite" aria-label="Review notifications">@for(item of notifications.items();track item.id){<article [class]="item.tone"><i></i><div><strong>{{item.title}}</strong><p>{{item.message}}</p></div><button type="button" aria-label="Dismiss notification" (click)="notifications.dismiss(item.id)">×</button></article>}</aside>@if(notifications.panelOpen()){<section class="history"><header><div><span>Activity</span><h2>Notifications</h2></div><button type="button" aria-label="Close notifications" (click)="notifications.togglePanel()">×</button></header>@if(!notifications.history().length){<p class="empty">No notifications yet.</p>}@for(item of notifications.history();track item.id){<div class="history-item" [class]="item.tone"><i></i><div><strong>{{item.title}}</strong><p>{{item.message}}</p></div></div>}</section>}`,styleUrl:'./notification-stack.component.scss'})
export class NotificationStackComponent{readonly notifications=inject(NotificationService)}
