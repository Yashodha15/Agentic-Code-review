import { Component,input } from '@angular/core';import { ReviewTrace } from '../../core/models';import { StatusPillComponent } from '../../ui/status-pill/status-pill.component';
@Component({selector:'review-agent-card',standalone:true,imports:[StatusPillComponent],template:`<article><header><b>{{lead().stage.slice(0,1).toUpperCase()}}</b><div><small class="role">Agent</small><h3>{{lead().stage}}</h3></div><ui-status-pill [status]="lead().status">{{lead().status}}</ui-status-pill></header><p>{{lead().detail}}</p>@if(children().length){<section>@for(child of children();track child.stage){<div><span>↳</span><em>Subagent</em><strong>{{child.stage.split('.')[1]}}</strong><ui-status-pill [status]="child.status">{{child.status}}</ui-status-pill></div>}</section>}@else{<footer>— No delegated tasks</footer>}</article>`,styleUrl:'./agent-card.component.scss'})export class AgentCardComponent{readonly lead=input.required<ReviewTrace>();readonly children=input.required<ReviewTrace[]>()}
